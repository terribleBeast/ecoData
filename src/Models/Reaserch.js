export default class Reaserch {
  constructor({ id, title, goal, startDate, endDate, status, reaserchers_id }) {
    this.id = id;
    this.title = title;
    this.goal = goal;
    this.startDate = startDate;
    this.endDate = endDate;
    this.status = status;
    this.reaserchers_id = reaserchers_id;
  }
}
